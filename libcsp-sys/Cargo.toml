[package]
name = "libcsp-sys"
version = "0.1.2"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["std", "alloc"]
std = []
alloc = []

zmq = ["std"]
usart = ["std"]
socketcan = ["std"]

[dependencies]

[build-dependencies]
bindgen = "0.69.0"
pkg-config = "0.3.27"
cmake = "0.1"
